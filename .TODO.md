# PR 1471 Feedback Resolution Progress

- [ ] Fix useless assignment `hasMore = false` before `break` in `functions/src/index.ts`.
- [ ] Revert relative path change for `ScholarshipData` import in `EligibilityStep.tsx`.
- [ ] Remove `website` fallback from `s.url || s.website` in `useOptionsData.ts` since it was updated to `url`.
- [ ] Revert `.gitignore` `node_modules/` change back to `/node_modules`.
- [ ] Remove duplicate `tmp/` from `.gitignore` since there is already a `/tmp` rule.
- [ ] Remove `school.degrees_awarded.predominant` from the `fields` query parameter in `functions/src/index.ts` since it's not used in the data mapping.
