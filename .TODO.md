# PR 1471 Feedback Resolution Progress

# PR 1471 Feedback Resolution Progress

- [x] Fix useless assignment `hasMore = false` before `break` in `functions/src/index.ts`.
- [x] Explain relative path change for `ScholarshipData` import in `EligibilityStep.tsx` to the reviewer.
- [x] Remove `website` fallback from `s.url || s.website` in `useOptionsData.ts` since it was updated to `url`.
- [x] Revert `.gitignore` `node_modules/` change back to `/node_modules`.
- [x] Remove duplicate `tmp/` from `.gitignore` since there is already a `/tmp` rule.
- [x] Remove `school.degrees_awarded.predominant` from the `fields` query parameter in `functions/src/index.ts` since it's not used in the data mapping.
