# Epic 4: Onboarding Wizard & Personalization

**Branch:** `ag/feat-onboarding-wizard`
**PR:** #1474

## Task Breakdown & Assignments

### Frontend Specialist: Unified Preferences & Discoverability
**Assignee:** @FrontendSpecialist
- [ ] Refactor `activeTab` into the left-hand Filter Sidebar (merge wizard preferences with standard `filterChips`).
- [ ] Create a robust React Hook abstracting `localStorage` (guest) and `Firestore` (authenticated) state sync.
- [ ] Build UI for Inline Banners / Empty States ("Unlock Your Personalized Feed") without blocking feed scanning.
- [ ] **Verification:** No infinite re-renders on the sidebar; Guest state saves locally; Auth state fetches from DB.

### Data Shield: Safe Migration & Rules
**Assignee:** @DataShield
- [ ] Update Firestore Security Rules ensuring users can only read/write their own `preferences` inside their user document.
- [ ] Audit and refine the `setDoc(..., { merge: true })` migration to prevent race conditions during Guest-to-Auth signup.
- [ ] **Verification:** Security rules reject unauthorized writes; no data loss upon merge.

### QA Explorer: Migration E2E
**Assignee:** @QAExplorer
- [ ] Write E2E tests validating the state migration from Guest (`localStorage`) to Authenticated (`Firestore`) during the signup event.
- [ ] Test Empty State branching in the filter sidebar to ensure manual search remains functional.
- [ ] **Verification:** Full test coverage on the migration flow.

### Inclusion Officer: Persona Check
**Assignee:** @InclusionOfficer
- [ ] Audit UX copy for "Unlock Your Feed" to ensure it doesn't simulate an anxiety-inducing "paywall".
- [ ] Audit Onboarding Wizard forms (Grades, State, Major, Ethnicity) for cultural sensitivity.
- [ ] **Verification:** Copy aligns with "Parent Co-Pilot" persona.

---

## Workflow Rules
- **Sync Protocol:** Always run `git pull origin ag/feat-onboarding-wizard --no-edit` before pushing.
- **Complex Conflicts:** Any merge conflict outside this `.TODO.md` file requires a HALT and User escalation.
- **Scratchpad:** Use `$(git rev-parse --show-toplevel)/tmp/` for CLI intermediate files.
