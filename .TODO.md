# PR 1471 Additional Feedback Resolution Progress

- [ ] Remove redundant data mapping in `src/lib/useOptionsData.ts`.
- [ ] Globally ignore `node_modules/` in `.gitignore` instead of just the root directory.
- [ ] Remove accidentally tracked `duplicates-report.json` from the repository.
