import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  palette: {
    background: {
      default: '#3C64B110',
    },
    primary: {
      main: '#3C64B1',
    },
  },
});
export default theme;
