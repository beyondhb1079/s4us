import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  palette: {
    background: {
      default: '#F3F6FA',
      secondary: '#303030',
    },
    primary: {
      main: '#3C64B1',
    },
  },
});
export default theme;
