rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: false
    }
    match /scholarships/{scholarshipId} {
    allow write: if
    	request.auth != null;
    }
  }
}